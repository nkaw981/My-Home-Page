{
	"name": "generic-cloudflare-worker",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.7.5",
		"vitest": "~3.0.7",
		"wrangler": "^4.10.0"
	},
	"dependencies": {
		"itty-router": "^5.0.18",
		"xmldom": "^0.6.0"
	}
}
